# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)
# <%= f.fields_for :locations do |addr| %>
#     <%= addr.label :city %>
#     <%= addr.text_field :city, value:@user.items.last.location.city if @user.items  %><br>
 
#     <%= addr.label :state %>
#     <%= addr.text_field :state, value:@user.items.last.location.state if @user.items  %><br>

#     <%= addr.label :zipcode %>
#     <%= addr.number_field :zipcode, value:@user.items.last.location.zipcode if @user.items  %><br>
#     <% end %>